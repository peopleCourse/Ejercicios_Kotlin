<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/gray_light">
<!--titulo principal-->
    <TextView
        android:id="@+id/tvTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/titulo_principal"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/azul_claro"
        android:padding="16dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/sub_titulo"
        android:textSize="16sp"
        android:textColor="@color/naranja"
        android:padding="16dp" />


    <!-- RecyclerView: equivalente Android de renderizar una lista con
         document.createElement + appendChild en un bucle, pero reciclando
         las vistas para que sea eficiente con listas largas -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCanciones"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="8dp" />

</LinearLayout>
